name: Auto-update site (smart index titles + sitemap + Google ping)

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Update index.html automatically
        run: |
          INDEX_FILE="index.html"
          PDF_DIR="pdfs"

          echo "🔍 Scan des fichiers PDF..."
          FILES=$(ls "$PDF_DIR"/*.pdf 2>/dev/null || true)

          # Supprime l'ancien bloc de la liste des numéros
          sed -i '/<h2>Numéros disponibles *:<\/h2>/,/<\/ul>/d' "$INDEX_FILE"

          # Crée un nouveau bloc HTML (ordre du plus ancien au plus récent)
          {
            echo '<h2>Numéros disponibles :</h2>'
            echo '<ul>'
            for FILE in $(ls "$PDF_DIR"/*.pdf | sort -V); do
              BASENAME=$(basename "$FILE")

              if [[ $BASENAME =~ ^N([0-9]+)_([0-9]{4})-([0-9]{2})\.pdf$ ]]; then
                NUM=$(echo "${BASH_REMATCH[1]}" | sed 's/^0*//') # supprime les zéros
                YEAR="${BASH_REMATCH[2]}"
                MONTH="${BASH_REMATCH[3]}"

                case $MONTH in
                  01) MONTH_NAME="Janvier" ;;
                  02) MONTH_NAME="Février" ;;
                  03) MONTH_NAME="Mars" ;;
                  04) MONTH_NAME="Avril" ;;
                  05) MONTH_NAME="Mai" ;;
                  06) MONTH_NAME="Juin" ;;
                  07) MONTH_NAME="Juillet" ;;
                  08) MONTH_NAME="Août" ;;
                  09) MONTH_NAME="Septembre" ;;
                  10) MONTH_NAME="Octobre" ;;
                  11) MONTH_NAME="Novembre" ;;
                  12) MONTH_NAME="Décembre" ;;
                esac

                echo "  <li><a href=\"pdfs/$BASENAME\" target=\"_blank\">Numéro $NUM – $MONTH_NAME $YEAR</a></li>"
              else
                echo "  <li><a href=\"pdfs/$BASENAME\" target=\"_blank\">$BASENAME</a></li>"
              fi
            done
            echo '</ul>'
          } > new_block.html

          # Supprime les anciennes lignes “À propos” et “Contact”
          sed -i '/À propos/d' "$INDEX_FILE"
          sed -i '/Contact/d' "$INDEX_FILE"

          # Insère la nouvelle liste juste avant la balise </footer>
          awk 'BEGIN{inserted=0}
               /<\/footer>/{system("cat new_block.html"); inserted=1}
               {print}
               END{if(inserted==0) system("cat new_block.html")}' "$INDEX_FILE" > tmpfile && mv tmpfile "$INDEX_FILE"

          # Réinsère “À propos” et “Contact” tout en bas
          {
            echo ''
            echo '<p style="text-align:center; margin-top:20px;">'
            echo '<a href="about.html">À propos</a> | <a href="contact.html">Contact</a>'
            echo '</p>'
          } >> "$INDEX_FILE"

          rm new_block.html

          echo "✅ Index mis à jour : ordre ancien→récent, sans zéros, liens en bas."

      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add index.html
          git commit -m "Auto-update index with latest PDF list" || echo "No changes to commit"
          git push origin main

      - name: Ping Google
        run: |
          curl "https://www.google.com/ping?sitemap=https://insoumisseineetloing.github.io/sitemap.xml"
