name: Auto-update site (smart index titles + sitemap + Google ping)

on:
  push:
    paths:
      - "pdfs/**"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Update index.html
        run: |
          INDEX_FILE="index.html"
          PDF_DIR="pdfs"

          echo "🔍 Scan des fichiers PDF..."
          FILES=$(ls "$PDF_DIR"/*.pdf 2>/dev/null || true)

          # Supprimer l'ancien bloc entre <h2>Numéros disponibles :<\/h2> et <\/ul>
          sed -i '/<h2>Numéros disponibles *:<\/h2>/,/<\/ul>/d' "$INDEX_FILE"

          # Ajouter le nouveau bloc
          {
            echo '<h2>Numéros disponibles :</h2>'
            echo '<ul>'
            # Liste triée du plus ancien au plus récent
            for FILE in $(ls -v "$PDF_DIR"/*.pdf); do
              BASENAME=$(basename "$FILE")

              if [[ $BASENAME =~ ^N([0-9]+)_([0-9]{4})-([0-9]{2})\.pdf$ ]]; then
                NUM="${BASH_REMATCH[1]#0}"  # Supprime le zéro initial
                YEAR="${BASH_REMATCH[2]}"
                MONTH="${BASH_REMATCH[3]}"

                case $MONTH in
                  01) MONTH_NAME="Janvier" ;;
                  02) MONTH_NAME="Février" ;;
                  03) MONTH_NAME="Mars" ;;
                  04) MONTH_NAME="Avril" ;;
                  05) MONTH_NAME="Mai" ;;
                  06) MONTH_NAME="Juin" ;;
                  07) MONTH_NAME="Juillet" ;;
                  08) MONTH_NAME="Août" ;;
                  09) MONTH_NAME="Septembre" ;;
                  10) MONTH_NAME="Octobre" ;;
                  11) MONTH_NAME="Novembre" ;;
                  12) MONTH_NAME="Décembre" ;;
                esac

                echo "  <li><a href=\"pdfs/$BASENAME\" target=\"_blank\">Numéro $NUM – $MONTH_NAME $YEAR</a></li>"
              else
                echo "  <li><a href=\"pdfs/$BASENAME\" target=\"_blank\">$BASENAME</a></li>"
              fi
            done
            echo '</ul>'
          } >> "$INDEX_FILE"

          echo "✅ Bloc <ul> régénéré dans l'ordre croissant et sans zéros !"

      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add index.html
          git commit -m "Auto-update index with latest PDF list" || echo "No changes to commit"
          git push origin main

      - name: Ping Google
        run: |
          curl "https://www.google.com/ping?sitemap=https://insoumisseineetloing.github.io/sitemap.xml"
